<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		
<mapper namespace="StockMapper">
	
	<!-- <sql id="selectProduct">
		SELECT prod_no
		FROM product
	</sql> -->
	<resultMap type="com.model2.mvc.service.domain.Stock" id="selectStockMap">
		<id property="stockNo" column="stock_no" jdbcType="NUMERIC"/>
		<result property="stocks" column="stocks" jdbcType="VARCHAR"/>
		<association property="product" javaType="com.model2.mvc.service.domain.Product">
			<id property="prodNo" column="prod_no" jdbcType="NUMERIC"/>
			<result property="prodName" column="prod_name" jdbcType="VARCHAR"/>
			<result property="prodDetail" column="prod_detail" jdbcType="VARCHAR"/>
			<result property="price" column="price" jdbcType="NUMERIC"/>
			<result property="regDate" column="reg_date" jdbcType="DATE"/>
			<result property="manuDate" column="manufacture_day" jdbcType="VARCHAR"/>
			<result property="fileName" column="image_file" jdbcType="VARCHAR" javaType="java.util.List"/>
		</association>
	</resultMap>
	
	<insert id="addStock" parameterType="com.model2.mvc.service.domain.Stock">
		INSERT
		INTO stock(stock_no, stocks, prod_no)
		VALUES (seq_stock_stock_no.nextVal,
					   #{stocks:NUMERIC},
					   #{product.prodNo:NUMERIC})
	</insert>
	
	<select id="getStock" parameterType="int" resultMap="selectStockMap">
		SELECT
		s.stock_no, s.stocks, p.prod_no
		FROM stock s, product p
		WHERE s.prod_no = p.prodNo AND p.prod_no = #{value}
	</select>
	
	
	<update id="updateStock" parameterType="com.model2.mvc.service.domain.Stock">
		UPDATE stock
		SET	stocks = #{stocks}
		<where>
			prod_no = #{product.prodNo}
		</where>
	</update>
	
	<delete id="deleteStock" parameterType="int">
		DELETE FROM stock
		WHERE prod_no = #{value}
	</delete>
</mapper>